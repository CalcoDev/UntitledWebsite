---
interface Props {}
---

<div class="strip left">
  {
    [1, 2, 3, 4, 5, 6].map((_) => (
      <a class="alt" href="/tudyjam">
        <img
          src="/tudyjam/dont_tudyjam_at_3_am.png"
          alt="Tudyjam invitation card"
        />
      </a>
    ))
  }
</div>
<div class="strip right">
  {
    [1, 2, 3, 4, 5, 6].map((_) => (
      <a class="alt" href="/tudyjam">
        <img
          class="invitation"
          src="/tudyjam/dont_tudyjam_at_3_am.png"
          alt="Tudyjam invitation card"
        />
      </a>
    ))
  }
</div>

<!-- Randomly offset the tudyjams -->
<script>
  let window_height = window.innerHeight;
  let left = document.querySelector(".left")!;
  let right = document.querySelector(".right")!;
  left.style.top = Math.ceil(Math.random() * window_height * 0.5) + "px";
  right.style.top = Math.ceil(Math.random() * window_height * 0.5) + "px";

  const checkWindowSize = () => {
    if (window.innerWidth < 952) {
      right.style.top += window_height * 0.5;
    }
  };

  window.addEventListener("resize", checkWindowSize);
</script>

<style>
  .left a {
    position: absolute;
    top: 0;
    left: 0;
    z-index: -1;
    pointer-events: auto;
  }

  .right a {
    position: absolute;
    top: 0;
    right: 0;
    z-index: 1;
    width:;
    max-width: 100vw;
  }

  img {
    max-width: 100vw;
  }

  .strip {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 60vh;
  }

  .left {
    position: absolute;
    left: 0;
  }

  .right {
    position: absolute;
    right: 0;
    float: right;
  }
</style>
