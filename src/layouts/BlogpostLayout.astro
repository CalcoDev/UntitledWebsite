---
import Layout from "./Layout.astro";
import type { BlogpostData } from "../data/types";

const { frontmatter } = Astro.props;

const data = frontmatter as BlogpostData;
---

<Layout title={frontmatter.title}>
  <main>
    <slot />
  </main>
</Layout>
